/var/log/llama-cpp/*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    create 0640 llama llama
    postrotate
        systemctl kill -s HUP llama-server.service >/dev/null 2>&1 || true
    endscript
}
